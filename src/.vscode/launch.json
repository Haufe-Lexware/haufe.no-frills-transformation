{
    "configurations": [
        {
            "type": "coreclr",
            "request": "launch",
            "name": "Launch NoFrillsTransformation",
            "program": "${workspaceFolder}/bin/Debug/net8.0/NoFrillsTransformation.dll",
            "args": ["-v"],
            "cwd": "${workspaceFolder}/bin/Debug/net8.0/",
            "stopAtEntry": false,
            "console": "internalConsole",
        }
    ],
    "inputs": [
        {
            "type": "promptString",
            "id": "programPath",
            "description": "Path to the program"
        }
    ]
}